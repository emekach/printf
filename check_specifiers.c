/**
 * check_specifier - check that characters is a valid specifier and
 * assigns an appropriate function for its printing.
 * @format: the specifier (char*)
 *
 * Return: pointer to a function
 */

int (*check_specifier(char*))(va_list)
{
	//expressions
}
